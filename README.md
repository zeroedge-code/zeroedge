# ZeroEdge — Portfolio (Skinned)

This build uses **all images** from `zeroedge-crypto-site.zip`:
- Extracted to `/assets`
- Background: `none detected`
- Logo: `none detected`

## Run
```
npm install
npm start
# http://localhost:3000
```
